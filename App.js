import React from "react";
import { Slot } from "expo-router"; // This is a special component from expo-router for routing
import firebase from "./firebaseConfig"; // Firebase initialization

export default function App() {
  return <Slot />;
}
